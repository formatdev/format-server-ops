services:
  broker:
    image: redis:7
    container_name: paperless-pcz-redis
    restart: unless-stopped
    volumes:
      - paperless_pcz_redis:/data
    networks:
      - backend

  db:
    image: postgres:16
    container_name: paperless-pcz-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - paperless_pcz_db:/var/lib/postgresql/data
    networks:
      - backend

  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: paperless-pcz
    restart: unless-stopped
    depends_on:
      - db
      - broker
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db
      PAPERLESS_DBNAME: ${POSTGRES_DB}
      PAPERLESS_DBUSER: ${POSTGRES_USER}
      PAPERLESS_DBPASS: ${POSTGRES_PASSWORD}
      PAPERLESS_URL: ${PAPERLESS_URL}
      PAPERLESS_ACCOUNT_DEFAULT_HTTP_PROTOCOL: ${PAPERLESS_ACCOUNT_DEFAULT_HTTP_PROTOCOL}
      PAPERLESS_COOKIE_PREFIX: ${PAPERLESS_COOKIE_PREFIX}
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY}
      PAPERLESS_TIME_ZONE: ${PAPERLESS_TIME_ZONE}
      PAPERLESS_OCR_LANGUAGE: ${PAPERLESS_OCR_LANGUAGE}
      PAPERLESS_TIKA_ENABLED: ${PAPERLESS_TIKA_ENABLED}
      PAPERLESS_TIKA_GOTENBERG_ENDPOINT: ${PAPERLESS_TIKA_GOTENBERG_ENDPOINT}
      PAPERLESS_TIKA_ENDPOINT: ${PAPERLESS_TIKA_ENDPOINT}
      USERMAP_UID: ${USERMAP_UID}
      USERMAP_GID: ${USERMAP_GID}
    volumes:
      - paperless_pcz_data:/usr/src/paperless/data
      - paperless_pcz_media:/usr/src/paperless/media
      - paperless_pcz_export:/usr/src/paperless/export
      - paperless_pcz_consume:/usr/src/paperless/consume
    networks:
      - backend
      - paperless
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.paperless-pcz.rule=Host(`${PAPERLESS_HOST}`)
      - traefik.http.routers.paperless-pcz.entrypoints=${TRAEFIK_ENTRYPOINT}
      - traefik.http.services.paperless-pcz.loadbalancer.server.port=8000

volumes:
  paperless_pcz_redis:
  paperless_pcz_db:
  paperless_pcz_data:
  paperless_pcz_media:
  paperless_pcz_export:
  paperless_pcz_consume:

networks:
  backend:
  paperless:
    external: true
  proxy:
    external: true
